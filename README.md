# Version Catalogs Example

### Check out these 2 branches for different ways to implement version catalogs.

`version-catalogs-1` - using `versionCatalogs` DSL

`version-catalogs-2` - using `libs.version.toml` file